import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const Color scaffoldBackgroundColor = Color(0xFFE0EFFF);
  static const Color primary = Colors.black;
  static const Color role = Color(0xFFFAF18D);
    static const Color date = Color(0xFF0056A2);
  static const Color secondary = Color.fromARGB(255, 255, 255, 255);
  // Accent and secondary

  static const Color backgroundColorAppBar = Colors.white;
  static const Color foregroundColorAppbar = Colors.black;
  static const Color scaffold = Color(0xFFFFFFFF);
  static const Color card = Color(0xFF8CBDB9);

  // Text colors
  static const Color tbles = Color(0xFF0056A2);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFF9E9E9E);

  // Border and divider
  static const Color border = Color.fromARGB(255, 187, 220, 255);
  static const Color divider = Color.fromARGB(255, 187, 220, 255);

  // Error and success

  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFFBC02D);

  // Disabled & input
  static const Color disabled = Color(0xFFBDBDBD);
  static const Color inputFill = Color(0xFFF1F1F1);
}
